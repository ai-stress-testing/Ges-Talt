# 2026-07-18 — verdict-loop + delegation convention (GT-10)

**Session/agent**: `pm/team-operations`.
**Issues touched**: GT-10; Audit-2 weaknesses 1, 2, 10.

## Done
- Added `agents/WORKFLOW.md`: the verdict loop (implementer → static
  review → empirical verification → PASS/FAIL, FAIL handback fields,
  3-attempt retry cap, attempt-4 auto-escalation to
  `pm/project-manager`), PM delegation rules (delivery-lead/
  program-tracker take altitude-appropriate sign-offs; PM keeps only
  spec ambiguity, cross-team conflict, scope changes, retry-cap
  escalations, access-widening), and the incident routing rule
  (`devops/sre` triages an ambiguous page first, hands off to
  `security/incident-responder` the instant malice is suspected).
- Pointed `CLAUDE.md`, `agents/pm/README.md`,
  `agents/devops/sre/SPEC.md`, and
  `agents/security/incident-responder/SPEC.md` at it instead of
  restating it.
- Distilled from `/workspace/agency-agents/strategy/coordination/handoff-templates.md`
  (source templates #2-#4: QA PASS/FAIL/escalation) — ported the field
  list, dropped the verbose markdown-table fill-in-the-blank format.

## Decisions
- Verdict is binary (PASS/FAIL) — no partial-credit or scored verdicts.
- Reality-checker's PASS is the acceptance sign-off; PM does not
  re-review passing work.
- IR owns an incident from the moment malice is suspected; SRE does not
  keep making live changes to the affected system after handoff.

## Blocked / carried
- None. GT-10 closed by this entry; backlog row update left to whoever
  owns `docs/backlog.md` edits (out of scope for this task).
